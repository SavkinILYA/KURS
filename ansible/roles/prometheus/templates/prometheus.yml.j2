global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'webservers'
    static_configs:
      - targets:
{% for host in groups['webservers'] %}
          - {{ hostvars[host].ansible_host }}:9100
{% endfor %}
